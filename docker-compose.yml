version: '2'

services:
  web-frontend:
    build:
      context: .
      dockerfile: ./docker/web/frontend/Dockerfile
    ports:
      - 80
    depends_on:
      - web-backend


  web-backend:
    build:
      context: .
      dockerfile: ./docker/web/backend/Dockerfile
    depends_on:
      - db-mysql

  
  db-mysql:
    image: mysql
    ports:
      - 3306
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - /data/mysq-bridge/datadir:/var/lib/mysql

networks:
  default:
    external:
      name: cplus_net     
